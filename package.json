{
  "name": "agriwise-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI-Powered Agriculture Crop Advisory Assistant SaaS MVP",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build:server": "npm run build --prefix server",
    "build:client": "npm run build --prefix client",
    "build": "npm run build --prefix client && node -e \"const fs = require('fs'); fs.cpSync('client/dist', 'dist', {recursive: true});\""
  },
  "keywords": [
    "agtech",
    "ai",
    "gemini",
    "supabase",
    "express",
    "react",
    "typescript",
    "tailwind"
  ],
  "author": "AgriWise Team",
  "license": "MIT"
}
